// Credits task
// ----------
// Add credits to the src files and build files.

// var gulp = require('gulp');
// var header = require('gulp-header');
// var fs = require('fs');
// var credits = fs.readFileSync('humans.txt', 'utf8');
// var config = require('../config').credits;

// gulp.task('credits', function () {
// 	return gulp.src(config.src)
// 		.pipe(header(credits))
// 		.pipe(gulp.dest(config.dest));
// });